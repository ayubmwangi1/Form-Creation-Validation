Simple Form Validation Example

This is a basic form validation script in HTML and JavaScript. 
It validates three fields: username, email, and password. The form will only be submitted if all fields are correct
